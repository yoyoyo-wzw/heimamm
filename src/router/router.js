// 导包
import Vue from 'vue'
import VueRouter from 'vue-router'

// 注册
Vue.use(VueRouter);

// 路由配置
const routes = [];

// 实例化
const router = new VueRouter({
    routes
})

// 暴露
export default router;